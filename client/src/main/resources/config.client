#
# Configuración para acceder al objeto en el servidor.
# Obtiene automáticamente un servidor disponible con el Locator.
#
# VotingService.Proxy=VotingService:tcp -h localhost -p 10000
Ice.Default.Locator=DemoIceGrid/Locator:default -h localhost -p 7777

# Configuración para el callback en localhost.
Callback.Endpoints=tcp -h localhost

# Ice.Trace.Network=3
# Ice.Trace.Protocol=1